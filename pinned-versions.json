{
  "comment": [
    "Release conditions for every exact-pinned version in package.json (dependencies, devDependencies and overrides),",
    "plus any override added to clear an npm audit advisory. An exact pin is invisible to update-npm-deps.ps1 - that",
    "script upgrades caret ranges and skips exact versions - so nothing would ever remind you a pin is stale. This file",
    "is what makes that check happen: on every run the script executes each `check` and compares its trimmed stdout to",
    "`expect`. Equal means the pin is still required; different means the condition that justified the pin has changed",
    "and the pin should be re-examined. `check: null` means no mechanical test exists - it is reported for manual review",
    "on every run instead of going quiet. Every exact pin must have an entry here, and every entry must name a package",
    "that is actually present in the declared section; the script reports either mismatch. See rule G100."
  ],
  "pins": {
    "@codemirror/state": {
      "section": "dependencies",
      "why": "obsidian's peerDependencies names this exact version. A second copy in the tree means two CodeMirror state instances at runtime, so editor extensions would attach to the wrong one.",
      "check": "node -e \"process.stdout.write(require('obsidian/package.json').peerDependencies['@codemirror/state'])\"",
      "expect": "6.5.0"
    },
    "@codemirror/view": {
      "section": "dependencies",
      "why": "Same as @codemirror/state - obsidian peer-pins it exactly, and the view layer must run against Obsidian's own CodeMirror instance.",
      "check": "node -e \"process.stdout.write(require('obsidian/package.json').peerDependencies['@codemirror/view'])\"",
      "expect": "6.38.6"
    },
    "moment": {
      "section": "dependencies",
      "why": "Must match the copy Obsidian bundles and re-exports as `moment`: obsidian's own dependencies name this exact version, and a different one would type against an API the runtime does not have.",
      "check": "node -e \"process.stdout.write(require('obsidian/package.json').dependencies.moment)\"",
      "expect": "2.29.4"
    },
    "@vitest/runner": {
      "section": "devDependencies",
      "why": "Declared only to hold the transitive runner at a known-good version; nothing imports it. The check records the version vitest itself depends on, so the pin stops being invisible when vitest moves.",
      "check": "node -e \"process.stdout.write(require('vitest/package.json').dependencies['@vitest/runner'])\"",
      "expect": "4.1.10",
      "manualCheck": "NOTE: the pin and this expectation already disagree - the declared pin is 4.1.0 while vitest depends on 4.1.10 - so npm is resolving a runner older than the vitest that drives it. The unit suite passes either way, and AGENTS.md records that the original reason for pinning (broken .d.ts under exactOptionalPropertyTypes) is now handled by tsconfig's skipLibCheck instead. Re-examine whether this pin is still needed at all; if not, drop the declaration and let vitest choose."
    },
    "js-yaml": {
      "section": "overrides",
      "why": "Astro and Starlight `import yaml from 'js-yaml'`, and js-yaml 5 is ESM-only with NO default export - hoisting 5.x into their subtree makes `astro build` die on 'The requested module js-yaml does not provide an export named default' before it reads a single page. The override exists to keep the whole tree off the vulnerable 3.x line, and 4.3.1 does that just as well while staying loadable. The check records the range astro itself asks for: when astro moves to 5.x, this pin can be dropped.",
      "check": "node -e \"process.stdout.write(require('astro/package.json').dependencies['js-yaml'])\"",
      "expect": "^4.3.0"
    },
    "obsidian": {
      "section": "devDependencies",
      "why": "These mocks reproduce one specific Obsidian API surface, so the type definitions they are written against are pinned rather than floated. The check records the newest published obsidian release: when it moves, the mocks should be re-examined against the new API rather than silently drifting.",
      "check": "npm view obsidian version",
      "expect": "1.13.1"
    },
    "typescript": {
      "section": "devDependencies",
      "why": "TypeScript 7 (tsgo) breaks this toolchain: @typescript-eslint's parser crashes on the native API, so type-aware ESLint cannot run on 7. @typescript-eslint peer-requires the range below, so 6.x is the last usable major. Hold until obsidian-dev-utils migrates.",
      "check": "node -e \"process.stdout.write(require('typescript-eslint/package.json').peerDependencies.typescript)\"",
      "expect": ">=4.8.4 <6.1.0"
    }
  }
}
