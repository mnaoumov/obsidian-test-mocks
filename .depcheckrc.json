{
  "comment": [
    "Read automatically by depcheck, which update-npm-deps.ps1 runs at the end of every sweep to report declared",
    "dependencies nothing appears to use. depcheck cannot see a package that is only invoked as a CLI from a script,",
    "named as a string in a config, or pulled in by a compiler option, so each name below is a verified false positive",
    "with the reference that proves it. Because this file exists, anything depcheck still reports fails the sweep -",
    "so a genuinely unused dependency cannot be waved through, and a new entry here needs its reason recorded.",
    "The key is `ignores`, depcheck's CLI-style rc name; `ignoreMatches` is the API-only spelling and is silently",
    "ignored in this file.",
    "",
    "@commitlint/cli - .husky/commit-msg runs it as the `commitlint` binary.",
    "@commitlint/config-conventional - extended by name (a string in the commitlint config's `extends`), never imported - either directly, or through obsidian-dev-utils' script-utils/commitlint-config, which extends it without declaring it.",
    "@vitest/runner - declared only to hold the transitive runner at a fixed version - nothing imports it. See pinned-versions.json.",
    "better-typescript-lib - has no import anywhere by design: it supplies the @typescript/lib-* aliases that tsconfig.json's `libReplacement: true` resolves.",
    "cspell - run as a CLI by scripts/spellcheck.ts, configured by cspell.json.",
    "czg - run as a CLI by scripts/commit.ts.",
    "dprint - run as a CLI by the format scripts, configured by dprint.json.",
    "husky - installed by scripts/prepare.ts; the hooks in .husky/ are its output.",
    "jest-environment-jsdom - selected by the `@jest-environment jsdom` docblock in the jest test files, which depcheck does not parse.",
    "linkinator - run as a CLI, named as a string by the markdownlint helper.",
    "markdownlint-cli2 - run as a CLI by the lint:md scripts, configured by .markdownlint-cli2.mjs.",
    "nano-staged - .husky/pre-commit runs it; configured by scripts/nano-staged-config.ts.",
    "obsidian-typings - never imported anywhere: scripts/helpers/obsidian-typings-surface.ts resolves the bare specifier through",
    "  ts.resolveModuleName and hands the resolved .d.ts to its own ts.Program. AGENTS.md L3 forbids importing it from src/ (the",
    "  `declare module 'obsidian'` augmentation activates globally), and 5.0.0 deleted the bridge layer that held the last real",
    "  import - so there is nothing left for depcheck to see. Removing it would break `npm run build:generate:typings-surface`",
    "  and src/obsidian/obsidian-typings-conformance.test.ts.",
    "typedoc-plugin-mdn-links - never imported; scripts/docs-gen/helpers/api-doc-link-rendering.ts reads",
    "  node_modules/typedoc-plugin-mdn-links/data/web-api.json at runtime for the MDN Web API link map."
  ],
  "ignores": [
    "@commitlint/cli",
    "@commitlint/config-conventional",
    "@vitest/runner",
    "better-typescript-lib",
    "cspell",
    "czg",
    "dprint",
    "husky",
    "jest-environment-jsdom",
    "linkinator",
    "markdownlint-cli2",
    "nano-staged",
    "obsidian-typings",
    "typedoc-plugin-mdn-links"
  ]
}
