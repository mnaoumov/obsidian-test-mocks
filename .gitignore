# vscode
.vscode

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

dist
coverage

# Astro documentation site
.astro
docs/dist
docs/src/content/docs/api
docs/src/generated-sidebar.json
docs/public/og

# Generated barrels (scripts/build-generate-indices.ts). Anchored under `src/` so it cannot swallow an
# authored `index.ts` elsewhere — `docs/src/components/api/index.ts` is one.
/src/**/index.ts
/src/globals/setup.ts

/tsconfig.tsbuildinfo
/.env

.claude/settings.local.json

