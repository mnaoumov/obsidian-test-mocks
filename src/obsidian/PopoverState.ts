/**
 * @file
 *
 * Mock of obsidian's `PopoverState` enum. The enum currently declares no members
 * in `obsidian.d.ts`, so the mock mirrors it as an empty enum.
 */

/**
 * Mock of Obsidian's `PopoverState` enum, which names the lifecycle states of a hover popover. Empty, like its
 * public declaration.
 */
export enum PopoverState {}
