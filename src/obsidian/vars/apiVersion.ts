/**
 * @file
 *
 * Mock of Obsidian's `apiVersion`, pinned to the `obsidian` typings version the mocks track.
 */

/**
 * The API version of the app, which follows the desktop app's release cycle.
 */
export const apiVersion = '1.13.1';
